"""
FlowerGame.ws -- WebSocket server package for the FlowerGame backend.

Contains FlowerWSServer, which serves the game dashboard, broadcasts game state,
and acts as the session orchestrator (creating/destroying game controllers in
response to dashboard actions).
"""
